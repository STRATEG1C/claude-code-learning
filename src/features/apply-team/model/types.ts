export interface TeamApplication {
  teamName: string;
  captainName: string;
  playerCount: string;
}

export interface TeamApplicationErrors {
  teamName: string;
  captainName: string;
  playerCount: string;
}
